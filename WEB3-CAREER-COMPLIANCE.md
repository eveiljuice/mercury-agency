# Проверка соответствия требованиям Web3.career API

## ✅ Требования соблюдены:

### 1. **rel="follow" используется правильно** ✅
- Строка 262 в `src/components/Web3Jobs.tsx`: `rel="follow"`
- Ссылки на web3.career в футере также имеют `rel="follow"`
- Нет использования `rel="nofollow"`

### 2. **Нет utm параметров в apply_url** ✅
- Строка 260: `href={job.apply_url}` - используется напрямую без модификаций
- Нет добавления параметров типа `utm_source`, `utm_medium`, `ref` и т.д.
- Проверено через grep - не найдено ни одного использования utm параметров

### 3. **Токен приватный и используется только на сайте** ✅
- **ИСПРАВЛЕНО**: Токен перенесен на бэкенд (`server/index.js`)
- Токен хранится в `.env` файле: `WEB3_CAREER_API_TOKEN`
- Создан прокси эндпоинт `/api/web3-jobs` на бэкенде
- Клиентский код больше не содержит токен
- Токен никогда не отправляется клиенту

## Изменения:

1. **Создан прокси эндпоинт** (`server/index.js`):
   - `GET /api/web3-jobs` - проксирует запросы к Web3.career API
   - Токен берется из переменных окружения
   - Параметры передаются как query parameters

2. **Обновлен клиентский код** (`src/services/web3Jobs.ts`):
   - Удален токен из клиентского кода
   - API запросы теперь идут через бэкенд прокси
   - URL: `http://localhost:3001/api/web3-jobs` (dev) или ваш домен (prod)

## Как проверить:

1. Убедитесь что в `.env` есть `WEB3_CAREER_API_TOKEN=kMn9WFPS2fhG2B38F9mqMh11VgepjAg7`
2. Запустите бэкенд: `npm run server`
3. Запустите фронтенд: `npm run dev`
4. Проверьте в DevTools что запросы идут на `/api/web3-jobs`, а не напрямую на web3.career
5. Проверьте что токен не виден в клиентском коде (View Source)

## Важно для продакшена:

В `src/services/web3Jobs.ts` строка 22 замените `'https://your-domain.com/api/web3-jobs'` на ваш реальный домен.

